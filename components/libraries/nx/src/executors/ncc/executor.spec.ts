// const options: NccExecutorSchema = {};

describe('Ncc Executor', () => {
  it('can run', async () => {
    // const output = await executor(options);
    // expect(output.success).toBe(true);
  });
});
